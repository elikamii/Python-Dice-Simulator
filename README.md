import random

# Define the minimum and maximum possible values for a standard die
MIN_VALUE = 1
MAX_VALUE = 6

# Simulate a single roll
roll_result = random.randint(MIN_VALUE, MAX_VALUE)

# Print the result
print(f"The result of the dice roll is: {roll_result}")Add basic script for a single dice roll simulation
